# This program asks user to enter the first and last name, with age, 
# and output the results

# get the user firstname
name = input('Enter the first and last name: ')

# get the user age
age = int(input('Enter the age: '))

# get the output
print(name + ', ' + str(age))
